extends layout

block content
    form(action="/login" method="post")
        input(type="text" name="email")
        br
        br
        input(type="password" name="password")
        br
        br
        input(type="submit")